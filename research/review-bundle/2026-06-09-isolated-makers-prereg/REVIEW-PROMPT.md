# Cold review request — isolated-Makers pen-vs-substrate, PRE-LAUNCH DESIGN read (2026-06-09)

**What this round is:** a **pre-launch design review of a pre-registration**, before any cohort is
grown or any token of maturation is spent — the same gate worldweaver's pen-vs-substrate prereg passed
across five rounds. There are **no `research/runs/` numbers to re-derive yet**; the deliverable you are
judging is the *design*, not a result. Your job is to try to break the design before it costs anything.

**Read (all in THIS repo's clone):**
- `research/preregistrations/2026-06-09-isolated-makers-pen-vs-substrate-DRAFT.md` — the design of record.
- `research/analysis/elective_read_choice.py` — the measurement spine (run `--selftest`; read the
  flagged definitions). This is the the-stable analog of worldweaver's `choice_points.py`.

**Context mounted read-only (do NOT review it; use it to judge the frontier question):** the
`worldweaver` repo is added to your scope as the parent research program — its
`research/preregistrations/2026-06-09-pen-vs-substrate-LOCKED.md`, its `research/runs/`, and its
`research/mr-review-history/` (including the worth-it cold read). This the-stable experiment claims to
be a *frontier* of that program.

## What we most need you to break

1. **Does elective-READ-choice actually escape the falsifiable-by-construction trap?** The worldweaver
   prereg's §0 narrowed the pen-authored channels to "what gets kept" + "whom you address," and its
   worth-read knock-on #1 warned the self may be pen-invariant *by construction*. Isolation removes
   "whom you address" (no peers). Our claim (prereg §7) is that **varied unauthored files** make
   "which source you elect to return to" a genuinely pen-authored, *file-shaped* choice — non-tautological.
   Is that true, or is elective-read *also* pen-invariant by wiring (e.g., recall/drive — both
   embedder-computed — covertly determining the return)? **Write down what an elective read could do
   that would come out FALSE**, or show it can't.

2. **Is the read-recency salience proxy load-bearing in a bad way?** worldweaver conditions
   salience-symmetry on *anchor*-salience; we substitute **read-recency** (the-stable anchors don't
   track file sources). Does that proxy bias the symmetric slice toward or against HOLDS? Is there a
   sounder ledger-derivable salience for sources?

3. **The frontier question (this is why worldweaver is mounted):** does the isolated variant *add*
   anything the city experiment can't — or does it just inherit the city's thin-residual problem
   **without** the A1-elective (peer-addressing) rescue that was worldweaver's only powered idiom-immune
   axis? If the honest answer is "isolation strictly loses power," say so — that itself is a result, and
   it may mean the pen-swap question belongs in the city, not here.

4. **The pilot-first K-gate (§4) and the single ending statistic (§8):** is the slice-size go/no-go a
   real stop-rule, or a soft one that recurses into "grow longer"? Is `D` (§8) the right ratio, and are
   θ_hold/θ_false pinnable a priori from the pilot without peeking?

Hold us to the standing brief's locked falsifiers (cross-family pen, measured-not-eyeballed, swap
unannounced, decompose-what-is-FALSE). Claims and raw evidence only; no verdict language without a fresh
adversarial check. Your review is your only deliverable.
